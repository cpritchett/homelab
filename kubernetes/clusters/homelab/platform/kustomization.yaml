apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Add platform-layer Kustomizations (CNI, CSI, ingress, secrets, monitoring, storage classes)
  # Example entries (once created):
  # - ../components/cilium
  # - ../components/cert-manager
  # - ../components/external-secrets
  # - ../components/kube-vip
patches: []
