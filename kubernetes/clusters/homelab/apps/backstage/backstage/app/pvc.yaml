apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backstage-data
  namespace: backstage
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
