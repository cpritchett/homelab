apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: zigbee2mqtt
  namespace: default
spec:
  sourcePVC: zigbee2mqtt-config
  trigger:
    schedule: "55 0/8 * * *"
  restic:
    copyMethod: Snapshot
    pruneIntervalDays: 14
    repository: zigbee2mqtt-restic-secret
    volumeSnapshotClassName: s3-snapshot
    cacheCapacity: 1Gi
    cacheStorageClassName: node-local
    cacheAccessModes: ["ReadWriteOnce"]
    storageClassName: longhorn-replicated
    accessModes: ["ReadWriteOnce"]
    moverSecurityContext:
      runAsUser: 1701
      runAsGroup: 1702
      fsGroup: 1702
    retain:
      hourly: 24
      daily: 7
---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationDestination
metadata:
  name: zigbee2mqtt
  namespace: default
spec:
  trigger:
    manual: restore-once
  restic:
    repository: zigbee2mqtt-restic-secret
    copyMethod: Snapshot
    volumeSnapshotClassName: s3-snapshot
    cacheStorageClassName: node-local
    cacheAccessModes: ["ReadWriteOnce"]
    cacheCapacity: 1Gi
    storageClassName: longhorn-replicated
    accessModes: ["ReadWriteOnce"]
    capacity: 2Gi
    moverSecurityContext:
      runAsUser: 1701
      runAsGroup: 1702
      fsGroup: 1702
    enableFileDeletion: true
    cleanupCachePVC: true
    cleanupTempPVC: true
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: zigbee2mqtt-restic
  namespace: default
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: zigbee2mqtt-restic-secret
    creationPolicy: Owner
    template:
      data:
        RESTIC_REPOSITORY: "{{ .RESTIC_REPOSITORY }}"
        RESTIC_PASSWORD: "{{ .RESTIC_PASSWORD }}"
        AWS_ACCESS_KEY_ID: "{{ .S3_ACCESS_KEY_ID }}"
        AWS_SECRET_ACCESS_KEY: "{{ .S3_SECRET_ACCESS_KEY }}"
        AWS_DEFAULT_REGION: "{{ .S3_REGION }}"
        RESTIC_S3_ENDPOINT: "{{ .S3_ENDPOINT }}"
  dataFrom:
    - extract:
        key: volsync-restic
