apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: nfs-backup
provisioner: nfs.csi.k8s.io
parameters:
  server: ${NFS_BACKUP_SERVER:?set NFS_BACKUP_SERVER}
  share: ${NFS_BACKUP_PATH:?set NFS_BACKUP_PATH}
mountOptions:
  - nfsvers=4.1
  - hard
  - rsize=1048576
  - wsize=1048576
  - noatime
reclaimPolicy: Delete
volumeBindingMode: WaitForFirstConsumer
allowVolumeExpansion: true
