apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: {{namespace}}
resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  {{#if longhornStorage}}
  - pvc-longhorn.yaml
  {{/if}}
  {{#if nfsStorage}}
  - pvc-nfs.yaml
  {{/if}}
  {{#if volsyncBackup}}
  - volsync.yaml
  {{/if}}
