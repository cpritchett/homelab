apiVersion: v1
kind: Service
metadata:
  name: {{appName}}
  namespace: {{namespace}}
  labels:
    app.kubernetes.io/name: {{appName}}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      name: http
      protocol: TCP
  selector:
    app: {{appName}}
