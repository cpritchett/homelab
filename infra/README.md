# infra/
Implementation details live here (configuration, exports, scripts).

This is intentionally light until the build-out begins. Do not treat this folder as normative policy.
Normative requirements live under `constitution/`, `contracts/`, and `requirements/`.
