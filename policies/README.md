# Policies

Repository-level policies enforced by CI guardrails.

## Repository Structure
- `repository/deny-unauthorized-root-files.rego` — Conftest policy preventing arbitrary files/directories in repo root
