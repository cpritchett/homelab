# PostgreSQL database configuration for Authentik
# Create in 1Password as "postgres.env" with tag "stack:authentik"
# Exported to: /mnt/apps01/secrets/authentik/postgres.env

# Database credentials (must match AUTHENTIK_POSTGRESQL__* values in authentik.env.example)
POSTGRES_DB=authentik
POSTGRES_USER=authentik
POSTGRES_PASSWORD=your-postgres-password
