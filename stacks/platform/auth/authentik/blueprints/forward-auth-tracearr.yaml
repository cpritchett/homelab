# yaml-language-server: $schema=https://goauthentik.io/blueprints/schema.json
# TOMBSTONE: Tracearr uses Plex auth natively — forward auth removed.
# Delete this file after blueprint is applied and resources are cleaned up.
version: 1

metadata:
  name: "Forward Auth - Tracearr"
  labels:
    blueprints.goauthentik.io/instantiate: "true"

entries:
  - model: authentik_core.application
    state: absent
    identifiers:
      slug: "tracearr"

  - model: authentik_providers_proxy.proxyprovider
    state: absent
    identifiers:
      name: "tracearr-forward-auth"
