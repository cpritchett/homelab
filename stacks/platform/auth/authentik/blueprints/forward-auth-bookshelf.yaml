# yaml-language-server: $schema=https://goauthentik.io/blueprints/schema.json
# Forward auth: Bookshelf (stacks/application/reading/compose.yaml)
version: 1

metadata:
  name: "Forward Auth - Bookshelf"
  labels:
    blueprints.goauthentik.io/instantiate: "true"

entries:
  - model: authentik_providers_proxy.proxyprovider
    state: present
    identifiers:
      name: "bookshelf-forward-auth"
    attrs:
      name: "bookshelf-forward-auth"
      mode: "forward_single"
      external_host: "https://bookshelf.in.hypyr.space"
      access_token_validity: "hours=24"
      refresh_token_validity: "days=30"
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]

  - model: authentik_core.application
    state: present
    identifiers:
      slug: "bookshelf"
    attrs:
      name: "Bookshelf"
      slug: "bookshelf"
      meta_launch_url: "https://bookshelf.in.hypyr.space"
      policy_engine_mode: "any"
      provider: !Find [authentik_providers_proxy.proxyprovider, [name, bookshelf-forward-auth]]
