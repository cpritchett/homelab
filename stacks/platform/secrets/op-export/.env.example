# 1Password secrets export environment variables
# Create this file at /mnt/apps01/secrets/op/op.env on the host
#
# PREREQUISITE: The 1Password CLI helper script must be staged on the host:
#   mkdir -p /mnt/apps01/scripts
#   curl -o /mnt/apps01/scripts/op-export-stack-env.sh \
#     https://raw.githubusercontent.com/cpritchett/homelab/main/stacks/scripts/op-export-stack-env.sh
#   chmod +x /mnt/apps01/scripts/op-export-stack-env.sh
#
# This script is shared infrastructure and copied to the host once, then reused by all stacks.

# 1Password service account token
# WARNING: Do NOT commit real service account tokens or other secrets to this example file.
OP_SERVICE_ACCOUNT_TOKEN=REPLACE_WITH_YOUR_SERVICE_ACCOUNT_TOKEN

# 1Password vault name
VAULT=homelab

# Destination directory for exported secrets
DEST_ROOT=/mnt/apps01/secrets
